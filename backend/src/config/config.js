const config = {
    schedulingAlgorithm: 'FCFS', // Algoritmo de escalonamento: FCFS, SJF (default), RR, etc.
    quantum: 10,
    maxProcesses: 100,
    processInterval: 1000,
};

// Todos os valores em milissegundos

export default config;